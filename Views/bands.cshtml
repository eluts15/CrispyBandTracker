@{
  Layout="Shared/_Layout.cshtml";
}

<h1>Happy BandTracker</h1>
<hr />

@if (Model.Count != 0)
{
  <ul>
    @foreach (var band in Model)
    {
      <li><a href='/bands/@band.GetId()'>@band.GetName()</a></li>
    }
  </ul>
}

<h4><a href="/bands/new">Add a new band</a></h4>
<h4><a href="/">Back To Main Page</a></h4>
